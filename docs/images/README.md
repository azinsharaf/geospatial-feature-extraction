# Images

This folder stores small documentation images that are rendered in GitHub READMEs.

Add the GIS overlay screenshot here:

- `docs/images/cars_gis_overlay.png`

Guidelines:

- Keep images reasonably small (e.g. < 2 MB) so cloning the repo stays fast.
- Avoid committing raw provider imagery if redistribution is restricted; prefer cropped or blurred examples.
